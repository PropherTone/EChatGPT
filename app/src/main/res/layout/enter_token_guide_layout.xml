<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white">

        <EditText
            android:id="@+id/token_input_box"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/input_box_rectangle"
            android:hint="@string/enter_token"
            android:importantForAutofill="no"
            android:inputType="text"
            android:padding="@dimen/content_padding"
            android:textSize="@dimen/content_text_size"
            app:layout_constraintBottom_toTopOf="@id/confirm_btn"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_chainStyle="packed" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/confirm_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/set"
            android:textColor="@color/white"
            android:textSize="@dimen/content_text_size"
            app:backgroundTint="@color/main_color"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@+id/token_input_box"
            app:layout_constraintStart_toStartOf="@+id/token_input_box"
            app:layout_constraintTop_toBottomOf="@+id/token_input_box" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>